{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "NODE_ENV=production node dist/index.js",
    "dev": "NODE_ENV=development tsx --env-file=.env --watch src/index.ts",
    "test": "NODE_ENV=test tsx --test --test-timeout=10000 tests/index.ts",
    "test:watch": "NODE_ENV=test tsx --test --test-timeout=10000 --watch tests/index.ts",
    "build": "rm -rf dist && tsc",
    "drop": "NODE_ENV=development tsx ./src/db/scripts/dropTables.ts",
    "migrate": "NODE_ENV=development tsx ./src/db/scripts/migrate.ts",
    "seed": "NODE_ENV=development tsx ./src/db/scripts/seed.ts",
    "db:reset": "npm run drop && npm run migrate && npm run seed"
  },
  "author": "frtavonatti",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/supertest": "^6.0.3",
    "supertest": "^7.1.4",
    "umzug": "^3.8.2"
  }
}
